- if policy(Like.new(bookmark: bookmark)).create?
  %div
    - if like2 = current_user.liked(bookmark)
      = link_to [topic, bookmark, like2], class: 'btn btn-danger', method: :delete do
        %i.glyphicon.glyphicon-star-empty> Unlike
    - else
      = link_to [topic, bookmark, Like.new], class: 'btn btn-primary', method: :post do
        %i.glyphicon.glyphicon-star> Like